import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  emptyContainer: {
    flex: 1,
    alignContent: "center",
    width: "100%",
    paddingVertical: 80,
    paddingHorizontal: 20,
    alignItems: "center",
  },

  textBold: {
    color: "#3f3d4e",
    fontSize: 14,
    fontWeight: "bold",
    padding: 6,
    marginTop: 20,
  },

  textRegular: {
    color: "#3f3d4e",
    fontSize: 14,
  },
});
